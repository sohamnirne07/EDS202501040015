quare = lambda x: x**2 #defining a lambda function to calculate the square of a number
n = float(input("enter the number : ")) #taking input from the user and converting it to a float
print(square(n)) #calling the lambda function and passing the user input as an argument, then printing the result